{
  "scripts": {
    "dev": "beefy ./index.js:bundle.js --live --open -- -t glslify",
    "build": "browserify index.js -o bundle.js -t glslify"
  },
  "dependencies": {
    "glslify": "~1.2.1",
    "ndarray": "~1.0.10",
    "gl-texture2d": "~0.1.11",
    "ndarray-fill": "^0.1.0",
    "vec2": "^1.5.0",
    "domready": "^1.0.5",
    "fc": "^0.2.0"
  },
  "devDependencies": {
    "beefy": "^1.1.0",
    "browserify": "^3.33.0"
  }
}
